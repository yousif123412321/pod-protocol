# PoD Protocol Environment Configuration
# Copy this file to .env for local development

# Solana Configuration
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com
ANCHOR_WALLET=~/.config/solana/id.json

# Alternative for testing (when no wallet available)
# ANCHOR_PROVIDER_URL=https://api.devnet.solana.com
# ANCHOR_WALLET=/tmp/test-keypair.json

# Light Protocol Configuration (for ZK Compression)
LIGHT_RPC_URL=https://devnet.helius-rpc.com/?api-key=your-api-key
PHOTON_INDEXER_URL=https://devnet.helius-rpc.com/?api-key=your-api-key

# Development Configuration
NODE_ENV=development

# Optional: Override default cluster
SOLANA_CLUSTER=devnet

# Optional: Custom program ID (defaults to devnet deployment)
# POD_PROGRAM_ID=HEpGLgYsE1kP8aoYKyLFc3JVVrofS7T4zEA6fWBJsZps

# IPFS Configuration (for metadata storage)
IPFS_GATEWAY_URL=https://ipfs.io/ipfs/

# Testing Configuration
TEST_TIMEOUT=60000
TEST_RETRIES=3